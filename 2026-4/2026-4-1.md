### 和集合 → |
```
[1, 1, 2, 3, 3] | [3 , 4, 5] # ← [1, 2, 3, 4, 5]  / 重複は取り除かれる
```

### 積集合 → &
```
[1, 2, 3, 4] & [3, 4, 5, 6] # ← [3, 4] / 両方の配列に共通する要素だけ抽出
```

### よくわからなかったこと
- 文字列配列 [ 正規表現 ] 
- 文字列の中から最初にマッチした部分を取り出す
```
str = "abc123def456"
str[/\d+/] # ← "123" / d+は数字が1つ以上
```
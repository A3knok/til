### 今日のコード
```
# 1
n, k = gets.split.map(&:to_i)
n.times do
    puts gets.to_i + k
end


# 2
n = gets.to_i
sum = 0

n.times { sum += gets.to_i }

puts sum
```

### 沼ったところ
- index使う時に0開始か1開始かで結果が変わってテストに通過しなかった

### メモ
- 配列に保存するとデータ量が多い場合メモリを使う
- #2みたく配列を使わずに書く方法を覚えておく。効率がいい。

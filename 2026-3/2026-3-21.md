### ハッシュを使うコツ
- 「違うのは“名前だけ”」→ ハッシュ
- 「処理が同じ」→ ハッシュ
- 「種類が増えそう」→ ハッシュ

### rails newで何している
- Railsアプリケーションの基本となるフォルダ構造を生成し、設定ファイルや初期コードもまとめて用意してくれるコマンドです。つまりすぐ開発を始められる最低限の雛形を作成してくれます。

### run --rm: コマンド実行後にコンテナを自動で削除
- コンテナ = アプリを動かすための一時的な環境
- 今回のコンテナ = rails new専用の作業場
- --rm = 役割が終わったから削除
### 今日のコード
```
n = gets.to_i
result = []

n.times do |i|
    a1, a2 = gets.split(' ')
    unless a1 == 'y' && a2 == 'y' # 可読性重視
        result << i + 1
    end
end

puts result.size
puts result
```
- 除外条件を見抜く
- 1件ずつ処理できるかを考える
- 状態数を減らす
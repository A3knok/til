### 文字列を任意の位置から任意の文字数取得する
```
num, n = gets.split.map(&:to_i)
str = gets.split
ok = true

(n - 1).times do |i|
  if str[i][-num, num] != str[i + 1][0, num]
    ok = false
    break
  end
end

puts ok ? "YES" : "NO"
```
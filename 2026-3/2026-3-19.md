### 整数除算の落とし穴
```5 / 2 #=> 2```
- 整数同士の計算では少数は切り捨てられる

### ceil が効かない理由
- すでに対象が整数の場合は意味がない
- (a.sum.to_f / n).ceilみたく小数点表示してから.ceilすると◎

### Rubyらしい書き方
- ifを減らす
```target ? a : b```

### nilの活用
-なければ0 を1行で表現
```target || 0```